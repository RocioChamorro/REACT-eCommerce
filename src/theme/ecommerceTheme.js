import { createTheme } from '@mui/material';

export const ecommerceTheme = createTheme({
  palette: {
    primary: {
      main: '#4abdacd9',
      contrastText: '#fff',
    },
    secondary: {
      main: '#F7B733',
      contrastText: '#fff',
    },
  }
})
