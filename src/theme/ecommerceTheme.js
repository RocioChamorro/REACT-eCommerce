import { createTheme } from '@mui/material';

export const ecommerceTheme = createTheme({
  palette: {
    primary: {
      main: '#FF637D',
    },
    secondary: {
      main: '#F4F1BB',
    },
  }
})
